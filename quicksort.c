#include <stdio.h>

int qsort(int array, int left, int right)
{
    arrayLength = sizeof(array);
    
    if (arrayLength <= 1) {
        return array;
    }
    
    pivot = array[random(arrayLength)];
    less = int array[arrayLength];
    more = int array[arrayLength];
    
    for (int i = 0; i < arrayLength; i++) {
        if (array[i] > pivot) {
            <#statements#>
        }
    }
}

int main(int argc, char *argv[])
{
    
}